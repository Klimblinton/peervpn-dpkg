server {
    listen 80;
    server_name {{ repo_hostname }};

    location / {
        root /opt/apt_repo;
        index index.html;
        autoindex on;
    }

    location ~ /(.*)/conf {
        deny all;  
    }

    location ~ /(.*)/db {
        deny all;
    }

}